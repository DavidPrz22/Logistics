For dashboard panel component: To generate or modify rate, only administrador and gerente can do it, so hide the generate rate button and modify tasa in the fronted, only visible for those users.
payment module: only visible by administrador and gerente
despacho module, only administrador can edit an order.