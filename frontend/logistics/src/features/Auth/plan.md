### Modify the login and register routes to correspond to the database model

1. Implement react hook forms and zod validation to the login/register requests
2. create schemas, and mutations
3. use the auth feature to manage these files
4. remove undefined and placeholder useAuth store and methods
5. align the registration fields with the user model (../../../../../backend/api/prisma/schema/usuario.prisma)